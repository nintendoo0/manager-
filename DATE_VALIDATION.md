# ✅ Валидация дат проектов

**Дата добавления**: 10 декабря 2025 г.

## 🎯 Проблема

Пользователи могли создавать проекты, где дата окончания раньше даты начала (например: начало 10.12.2025, окончание 08.12.2025), что не имеет смысла.

## ✨ Решение

Добавлена комплексная валидация дат на фронтенде и бэкенде.

---

## 🔒 Правила валидации

### Обязательные правила:

1. ✅ Дата окончания НЕ может быть раньше даты начала
2. ✅ Дата начала обязательна
3. ✅ Дата окончания опциональна (можно оставить пустой)
4. ✅ Даты могут быть равны (проект на один день)

### Разрешённые сценарии:

```
Начало: 10.12.2025, Окончание: 20.12.2025  ✅ OK
Начало: 10.12.2025, Окончание: 10.12.2025  ✅ OK (одинаковые даты)
Начало: 10.12.2025, Окончание: (пусто)     ✅ OK (бессрочный проект)
```

### Запрещённые сценарии:

```
Начало: 10.12.2025, Окончание: 08.12.2025  ❌ ОШИБКА
Начало: (пусто),    Окончание: 20.12.2025  ❌ ОШИБКА
```

---

## 🛠 Что изменилось

### Frontend (ProjectForm.js)

1. **Функция валидации**:
```javascript
const validateDates = () => {
  if (start_date && end_date) {
    const startDate = new Date(start_date);
    const endDate = new Date(end_date);
    
    if (endDate < startDate) {
      setError('Дата окончания не может быть раньше даты начала проекта');
      return false;
    }
  }
  return true;
};
```

2. **Атрибут min для поля даты**:
```html
<input
  type="date"
  name="end_date"
  min={start_date}  <!-- Ограничение выбора на уровне браузера -->
/>
```

3. **Визуальная индикация ошибки**:
```jsx
{start_date && end_date && new Date(end_date) < new Date(start_date) && (
  <small style={{ color: '#e74c3c' }}>
    ⚠️ Дата окончания не может быть раньше даты начала
  </small>
)}
```

### Backend (projectController.js)

**createProject**:
```javascript
// Валидация дат
if (start_date && end_date) {
  const startDate = new Date(start_date);
  const endDate = new Date(end_date);
  
  if (endDate < startDate) {
    return res.status(400).json({ 
      message: 'Дата окончания проекта не может быть раньше даты начала' 
    });
  }
}
```

**updateProject** - аналогичная валидация

---

## 🧪 Тесты

### Файл: `project-validation.unit.test.js`

**7 тестов**:

1. ❌ Создание проекта с некорректными датами → 400 Bad Request
2. ✅ Создание проекта с корректными датами → 201 Created
3. ✅ Создание проекта с одинаковыми датами → 201 Created
4. ✅ Создание проекта без даты окончания → 201 Created
5. ❌ Создание проекта без названия → 400 Bad Request
6. ❌ Обновление проекта с некорректными датами → 400 Bad Request
7. ✅ Обновление проекта с корректными датами → 200 OK

### Запуск тестов:

```powershell
cd c:\manager-\backend
npm test -- tests/unit/project-validation.unit.test.js
```

**Ожидаемый результат**: 7/7 тестов проходят ✅

---

## 📱 UX улучшения

### 1. Атрибут min

Поле "Дата окончания" получает атрибут `min={start_date}`, что:
- Блокирует выбор дат раньше начала в календаре
- Работает на уровне браузера (HTML5)
- Мгновенная обратная связь пользователю

### 2. Визуальное предупреждение

Если пользователь вручную ввёл некорректную дату:
- Появляется красное предупреждение ⚠️
- Сообщение понятное и конкретное
- Кнопка "Сохранить" заблокируется при попытке отправки

### 3. Сообщения об ошибках

**Frontend**:
```
"Дата окончания не может быть раньше даты начала проекта"
```

**Backend**:
```
"Дата окончания проекта не может быть раньше даты начала"
```

---

## 🔄 Как это работает

### Сценарий 1: Пользователь создаёт проект

```
1. Выбирает дату начала: 10.12.2025
2. Поле "Дата окончания" теперь не позволяет выбрать даты до 10.12
3. Пользователь пытается ввести 08.12.2025 вручную
4. Появляется красное предупреждение
5. При попытке сохранить → ошибка валидации, форма не отправляется
```

### Сценарий 2: Обход фронтенда (прямой запрос к API)

```
1. Хакер отправляет POST /api/projects с датами:
   start_date: 2025-12-10
   end_date: 2025-12-08
2. Backend проверяет даты
3. Возвращает 400 Bad Request с понятным сообщением
4. Проект НЕ создаётся в БД
```

---

## 📊 Изменённые файлы

| Файл | Что добавлено |
|------|---------------|
| `frontend/src/components/Projects/ProjectForm.js` | Функция validateDates, атрибут min, визуальное предупреждение |
| `backend/controllers/projectController.js` | Валидация в createProject и updateProject |
| `backend/tests/unit/project-validation.unit.test.js` | 7 новых тестов |

---

## 🚀 Как протестировать

### Ручное тестирование:

1. Откройте форму создания проекта
2. Введите дату начала: `10.12.2025`
3. Попробуйте выбрать дату окончания: `08.12.2025`
4. ✅ Должно появиться предупреждение
5. Попробуйте сохранить
6. ✅ Должна появиться ошибка

### Автоматическое тестирование:

```powershell
cd c:\manager-\backend
npm test -- tests/unit/project-validation.unit.test.js
```

---

## 🎨 Визуальный пример

**До валидации**:
```
Начало: [10.12.2025] ✓
Окончание: [08.12.2025] (нет предупреждения)
[Сохранить] ← Создаст некорректный проект
```

**После валидации**:
```
Начало: [10.12.2025] ✓
Окончание: [08.12.2025] 
⚠️ Дата окончания не может быть раньше даты начала
[Сохранить] ← Покажет ошибку
```

---

## 🔍 Дополнительные проверки

### Также добавлена валидация:

1. **Название проекта обязательно**
   - Пустое название → 400 Bad Request
   - "Название проекта обязательно"

2. **Формат даты**
   - Backend принимает: YYYY-MM-DD
   - Автоматическое преобразование из DD.MM.YYYY

---

## 📚 Связанные материалы

- **WORK_REPORT.md** - общий отчёт о проекте
- **TEST_REPORT.md** - результаты всех тестов
- **QUICK_START_DASHBOARD.md** - как использовать систему

---

## ✅ Чек-лист готовности

- [x] Валидация на фронтенде
- [x] Валидация на бэкенде
- [x] Атрибут min для HTML5
- [x] Визуальное предупреждение
- [x] 7 юнит-тестов
- [x] Понятные сообщения об ошибках
- [x] Документация

---

**Версия**: 1.0  
**Статус**: ✅ Готово к использованию
